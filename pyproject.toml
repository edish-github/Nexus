[project]
name = "nexus"
version = "0.1.0"
description = "NEXUS"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "psycopg[binary,pool]",
    "numpy",
    "boto3",
    "langchain",
    "langchain-aws",
    "pydantic",
    "pydantic-settings",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "ruff",
    "pytest",
]

# NEXUS is an application, not a distributable library: the Lambda code ships
# via the SAM layer in layers/shared, so uv must not try to build this repo.
[tool.uv]
package = false

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "tests",
]

[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = [".venv", "frontend", "docs"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
